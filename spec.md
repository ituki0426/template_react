# 仕様書

## 1. メニューバー

- どのページでも表示される共有コンポーネントとする。
- 回答者ページ（`/answer`）と管理者ページ（`/admin`）へ切り替えられるようにする。

## 2. 回答者ページ `/answer`

### 機能

- 名前・メールアドレス・満足度（5 段階）・自由記述欄のあるイベントアンケートフォームを表示する。
- 満足度が **3 以下（1・2・3 を含む）** の場合のみ、「改善してほしい点」の追加テキスト欄を表示する。4 または 5 の場合は当該欄は表示しない（保存時も値を持たない、または空文字とする）。
- 送信前に確認画面を挟み、確定操作のあとで回答を永続化する（[4. データモデルと永続化](#4-データモデルと永続化)）。

### デザイン

- 全体をカード型レイアウトとし、配色はブルー系で統一する。
- レスポンシブ対応とし、スマートフォンでも見やすくする。

## 3. 管理者ページ `/admin`

- 回答一覧を **提出日時つき** で表示する。個別削除と全削除の操作を提供する（全削除・個別削除の前に確認ダイアログを出すこと）。
- 画面上部に、満足度の **平均スコア** と **回答数** のサマリーカードを表示する。
- 満足度の **分布**（1〜5 の件数）を棒グラフで表示する。
- 管理画面に **簡易認証** をつける（詳細は [4.4 簡易認証とデータの関係](#44-簡易認証とデータの関係)）。

## 4. データモデルと永続化

### 4.1 回答レコード（1 件）

1 回の回答送信で、次のフィールドを持つオブジェクトを 1 件として扱う。

| フィールド | 型・制約 | 必須 | 備考 |
| ---------- | -------- | ---- | ---- |
| `id` | 文字列（UUID 等） | はい | クライアント側で生成してよい。 |
| `submittedAt` | ISO 8601 形式の文字列 | はい | 例: `2025-03-22T14:30:00.000Z`。一覧の日時表示に用いる。 |
| `name` | 文字列 | はい | |
| `email` | 文字列 | はい | メール形式のバリデーションはフォーム（UI）側の要件とする。 |
| `satisfaction` | 整数 1〜5 | はい | 平均・分布グラフの集計の元。 |
| `freeText` | 文字列 | 任意 | 自由記述。未入力は空文字でよい。 |
| `improvementText` | 文字列 | 条件付き | **満足度が 3 以下のときのみ** 入力・保存する。4・5 のときは空文字とする（キー省略は実装次第だが、意味としては「値なし」で統一する）。 |

**JSON 例（満足度 2 で改善文あり）**

```json
{
  "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "submittedAt": "2025-03-22T14:30:00.000Z",
  "name": "山田太郎",
  "email": "yamada@example.com",
  "satisfaction": 2,
  "freeText": "楽しかったです。",
  "improvementText": "会場の空調を調整してほしいです。"
}
```

**JSON 例（満足度 4 のとき `improvementText` は空）**

```json
{
  "id": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
  "submittedAt": "2025-03-22T15:00:00.000Z",
  "name": "佐藤花子",
  "email": "sato@example.com",
  "satisfaction": 4,
  "freeText": "",
  "improvementText": ""
}
```

### 4.2 永続化方式（MVP）

- **保存先**: ブラウザの `localStorage` に、回答オブジェクトの **配列** を JSON 文字列化して保存する。
- **キー名**: `event-survey-answers-v1` のように、プレフィックスに **バージョン接尾辞**（`v1`）を付ける。スキーマ変更時は `v2` など新キーへ移行する方針とし、実装側で必要なら移行処理を書く。
- **制約の明示**: データは **ブラウザ・端末ごと** に分かれ、他者と共有されない。プライベートブラウズやストレージ削除で消える場合がある。本番で複数端末・複数ユーザーで集計する必要がある場合は、後述のとおりバックエンドへ差し替える。

**将来拡張（参考）**: HTTP API とサーバ側データベースに置き換える場合、上記フィールドをリソースの形としてそのまま利用できるようにする。

### 4.3 ストレージに対する操作（仕様上の契約）

実装時のモジュール名は任意。振る舞いは次のとおりとする。

- **一覧取得**: 保存済みの配列を読み、`submittedAt` の **新しい順（降順）** で並べた一覧として返す。管理者の回答一覧表示と一致させる。
- **追加**: 回答者が確認画面で確定したタイミングで、生成した 1 件を配列末尾に追加して保存する（並びは一覧取得時に降順にそろえる）。
- **個別削除**: 指定した `id` のレコードを除いた配列を保存する。
- **全削除**: 配列を空にして保存する。

### 4.4 簡易認証とデータの関係

- 認証は **管理者ページの閲覧・操作の入り口** とする。回答データのストレージキーは **回答用と分離せず同一** でよい（認証済みのセッション中だけ `/admin` で一覧・削除・グラフを触れる）。
- **パスワードを `localStorage` に平文保存しない。**
- **推奨する MVP の方式**: ビルド時に埋め込む環境変数（例: Vite の `VITE_ADMIN_PASSWORD`）と、画面で入力されたパスワードを **照合** する。一致した場合のみ `sessionStorage` にログイン済みフラグ（例: `event-survey-admin-auth` = `"1"`）を立て、ページ再読込まで管理画面を表示する。不一致時はフラグを立てず、パスワード入力を求める。
- **注意**: 上記はプロトタイプ向けであり、パスワードがクライアントに含まれるため **本番の機密保護には不向き** である。本番ではサーバ側認証とセッション管理に置き換えること。
